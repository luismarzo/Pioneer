<?xml version="1.0"?>
<launch>
  <!-- send table.xml to param server -->
  <param name="robot_description" command="$(find xacro)/xacro $(find pioneer)/robots/pioneer3at.xacro --inorder" />

</launch>

